package constants_test

import (
	. "github.com/onsi/ginkgo/v2"

	// Verify the package is importable
	_ "virtwork/internal/constants"
)

var _ = Describe("Constants", func() {
	It("should be importable", func() {
		// Package import above verifies the package compiles and is importable.
		// Constant value assertions will be added in Phase 1.
	})
})
